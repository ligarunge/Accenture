function changeCellContent() {
    const rowNumber = document.getElementById('row-number').value;
    const colNumber = document.getElementById('col-number').value;
    const 

    const row = document.querySelector(`#sampleTable tr:nth-child(${rowNumber})`);
    row.querySelector(`td.nth-child(${colNumber})`);

    if(!col) {
        alert()
    }
}